# nginx snippet to serve runtime-config.json directly (if using static export)
location /runtime-config.json {
  add_header Content-Type application/json;
  alias /usr/share/nginx/html/runtime-config.json;
}
